import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['assets.aceternity.com', 'img.olympics.com', 'cdn.discordapp.com', 'ui.aceternity.com'],
  },
};

export default nextConfig;
